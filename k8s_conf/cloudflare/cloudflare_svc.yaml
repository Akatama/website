apiVersion: v1
kind: Service
metadata:
  name: cloudflared-health
  namespace: cloudflare-tunnel
spec:
  selector:
    app: cloudflared
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
